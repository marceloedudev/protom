export const Conflict = {
    description: "Conflict",
    content: {
        "application/json": {
            schema: {
                $ref: "#/schemas/Exception",
            },
        },
    },
};
